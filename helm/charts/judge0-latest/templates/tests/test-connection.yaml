apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "judge0.fullname" . }}-test-connection"
  labels:
    {{- include "judge0.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "judge0.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
